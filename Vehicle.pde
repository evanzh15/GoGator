class Vehicle {
  //Car, rts bus, or moped
  int type;
  float speed;
  float xpos;
  Vehicle(int type, float speed, float xpos) {
    this.type = type;
    this.speed = speed;
    this.xpos = xpos;
  }
  
  void drawVehicle(int type, float xpos) {
    if (type == 0) {
      //Implement
    }
    else if (type == 1) {
      //Implement
    }
    else if (type == 2) {
      //Implement
    }
    
  }
}
